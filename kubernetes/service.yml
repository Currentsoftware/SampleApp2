apiVersion: v1
kind: Service
metadata:
  name: sampleapi-svc
  labels:
    app: sampleapi
spec:
  type: NodePort
  ports:
  -  port: 8080
     nodePort: 30000
     protocol: TCP
  selector:
    app: sampleapi